// Json and apis 

// destrucuring 

const myObj = {
    name: "Aditya",
    age: 25
}

const name = myObj.name;
const age = myObj.age;

const {name : kohli } = myObj; //acghaa kohli mtlb uska hamne naya naam de diya hai var name ke liye

console.log(kohli);